[pytest]
addopts = -m "not integration and not gemini_live"
markers =
    integration: requires internet access and clones a public GitHub repository
    gemini_live: requires explicit opt-in and an external Gemini API credential
